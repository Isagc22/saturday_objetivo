<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tableros</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css" />
  <link rel="stylesheet" href="/css/nav.css" />
  <link rel="stylesheet" href="/css/base.css" />
  <link rel="stylesheet" href="/css/tableros.css">
<link rel="stylesheet" href="/css/formRegistroReq.css">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/simplebar@6.2.5/dist/simplebar.min.css" />
  
</head>

<body class="light-mode">
  <nav class="nav-info-prin d-flex justify-content-between align-items-center p-3">
    <!-- Botón menú -->
    <button class="btn btn-panel-tableros" type="button" data-bs-toggle="offcanvas"
      data-bs-target="#offcanvasWithBothOptions" aria-controls="offcanvasWithBothOptions">
      <i class="bi bi-arrow-right-circle icon-barra-lateral"></i>
    </button>

    <!-- 🔍 Buscador transversal -->
    <!-- SEARCH (nav) -->
    <form id="searchForm" class="d-flex mx-3 flex-grow-1 position-relative" style="max-width: 420px">
      <input id="searchInput" class="form-control me-2" type="search" placeholder="Buscar solicitud..."
        autocomplete="off" aria-label="Buscar" />
      <!-- Cambié a type="button" para que NO haga submit por defecto -->
      <button id="searchBtn" class="btn btn-outline-dark" type="button">
        <i class="bi bi-search"></i>
      </button>

      <!-- Dropdown dinámico -->
      <ul id="searchResults" class="list-group position-absolute w-100 shadow" style="
            top: 100%;
            z-index: 1050;
            display: none;
            max-height: 250px;
            overflow-y: auto;
          "></ul>
    </form>

    <!-- Modal para detalle del requerimiento -->
    <%- include('../partials/modalDetalleRequerimiento.ejs') %>

      <!-- Cambiar tema -->
      <div class="d-flex gap-3 align-items-center">
        <span class="fs-4"><i class="bi bi-sun"></i></span>
        <div class="toggle" id="cambioTema">
          <div class="circle"></div>
        </div>
        <span class="fs-4"><i class="bi bi-moon"></i></span>
      </div>

      <!-- Cerrar sesión -->
      <button id="logoutBtn" class="btn btn-danger">
        <i class="bi bi-box-arrow-right"></i> Cerrar sesión
      </button>
  </nav>

  <!-- PANEL LATERAL -->
  <%- include('./panelLateral.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/panelTableros.js"></script>
</body>

</html>